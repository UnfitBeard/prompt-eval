services:
  api:
    build: .
    container_name: prompt_edu_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./static:/app/static
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

